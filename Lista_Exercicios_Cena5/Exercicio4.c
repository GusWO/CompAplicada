#include <stdio.h>

#define TAM 8

int main(int argc, char const *argv[])
{
    int vet[TAM];
    int soma = 0;

    for (int i = 0; i < TAM; i++)
    {
        printf("Enche o vetor ai: ");
        scanf("%d", &vet[i]);
        printf("\n");

    }

    printf("Digite os valores das posições dos nros que deseja somar: ");
    scanf("%d", &i)

    
    
}
